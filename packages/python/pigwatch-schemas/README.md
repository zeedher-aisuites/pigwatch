# pigwatch-schemas

Transport-neutral, versioned schemas shared across PigWatch components. M1 adds the strict version
`1.0` observation envelope and typed scalar contract fixtures while preserving M0 source identity
and orthogonal origin/delivery provenance. These fixtures validate transport and persistence; they
do not implement sensor behavior.
